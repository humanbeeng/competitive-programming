#include <bits/stdc++.h>

using namespace std;


int main(){
    int t = 1;
    cin >> t;

    while (t--) {
        int n;
        int in;
        vector<int> vi;
        for(int i=0; i<n;i++){
            cin >> in;
            vi.push_back(in);
        }

        

 
    }

    return 0;
}